# Detector training only. Do NOT add paddlepaddle or ollama here —
# this repo is deliberately isolated from the comparison pipeline's stack.
ultralytics>=8.3,<9.0
torch>=2.2
opencv-python-headless>=4.9
pyyaml>=6.0
pandas>=2.2
matplotlib>=3.8
tqdm>=4.66
pytest>=8.0
